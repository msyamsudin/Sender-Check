{
  "name": "@sender-check/console",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "description": "Skrip konsol untuk memverifikasi selector adapter terhadap Gmail sungguhan, tanpa manifest dan tanpa signing.",
  "license": "MIT",
  "dependencies": {
    "@sender-check/adapters": "workspace:*",
    "@sender-check/core": "workspace:*"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  },
  "scripts": {
    "build": "node build.ts"
  }
}
